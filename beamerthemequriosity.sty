\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{beamerthemequriosity}[2024/12/06 v0.1.0 Quriosity Beamer theme]

\mode<presentation>

% Theme options

\usecolortheme{quriosity}
\usefonttheme{quriosity}
\useinnertheme{quriosity}
%\useoutertheme{quriosity}
\useoutertheme{miniframes}

% Options
% TODO: add options, like whether to add transparency for the color font, or add a light (as in not dark) mode

% Packages
\RequirePackage{fontspec}
\setsansfont{blinkmacsystemfont}[
  Path=./BlinkMacSystemFont/otf/,
  Extension=.otf,
  UprightFont=*-regular,
  BoldFont=*-bold,
  ItalicFont=*-regularitalic,
  BoldItalicFont=*-bolditalic,
]
\usefonttheme[onlymath]{serif}
% Remove navigation symbols
\setbeamertemplate{navigation symbols}{}

% TODO: look at second mode to mimic PowerPoint's layout with two screens
\mode<all>
